
storage:

  example_data:
    storageType: LOCAL_STORAGE
    storageConfig:
      rootPath: examples/models/python/data

storageSettings:
  defaultStorage: example_data
  defaultFormat: CSV

sparkSettings:

  sparkConfig:
    spark.cores.max: 4
    spark.driver.memory: 2G
    spark.executor.memory: 2G
    spark.default.parallelism: 8
    spark.sql.shuffle.partitions: 8
