/**
 * A component that shows a range of charts.
 * @module
 * @category Component
 */

import React from "react";
// import {HighchartsGeneral} from "./Highcharts/HighchartsGeneral";

export const ChartTs = (props: any) => {

    return (
        <React.Fragment>
            {/*{HighchartsGeneral && props.savedChartState.chartType === "line" &&*/}
            {/*    <HighchartsGeneral {...props}/>*/}
            {/*}*/}

            {/*{HighchartsGeneral && props.savedChartState.chartType === "scatter" &&*/}
            {/*    <HighchartsGeneral {...props}/>*/}
            {/*}*/}

            {/*{HighchartsGeneral && props.savedChartState.chartType === "area" &&*/}
            {/*    <HighchartsGeneral {...props}/>*/}
            {/*}*/}

            {/*{HighchartsGeneral && props.savedChartState.chartType === "spline" &&*/}
            {/*    <HighchartsGeneral {...props}/>*/}
            {/*}*/}

        </React.Fragment>
    )
};