import React from "react";

/**
 * A component that shows a message if no flow is provided to the FLowSvg component.
 */

const NoFlow = () => {

    return (

        <div className={"my-auto text-muted align-items-center w-100 text-center"}>
            No flow selected
        </div>
    )
};

export default NoFlow;